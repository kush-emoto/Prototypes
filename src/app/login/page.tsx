"use client";
import { useState } from "react";
export default function LoginPage(){const[mode,setMode]=useState<"login"|"signup"|"otp">("login");return <div className="design-auth"><div className="auth-copy"><h1>Experience the Future<br/>of Electric Mobility</h1><p>🟢🟠⚪　1.5 Lakh+ Happy Riders</p><button>Request call back</button></div><section>{mode==="otp"?<><button className="close" onClick={()=>setMode("login")}>×</button><h2>Verify OTP</h2><div className="otp-boxes">{[1,2,3,4,5,6].map(i=><input key={i} maxLength={1}/>)}</div><button className="auth-primary">Continue</button></>:<><h2>{mode==="login"?"Login":"Sign up"}</h2><p>Experience the Future of Electric Mobility</p><input placeholder="Phone number"/><>{mode==="signup"&&<input placeholder="Name"/>}</><button className="auth-primary" onClick={()=>setMode("otp")}>{mode==="login"?"Get OTP":"Signup"}</button><div className="or">or</div><button className="google">Continue with google</button><small>{mode==="login"?"Don’t have an account? ":"Already have an account? "}<button onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Sign up":"Login"}</button></small></>}</section></div>}
